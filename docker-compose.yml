services:
  clearme-web:
    build:
      context: .
      dockerfile: Dockerfile.web
    image: clearme-web:latest
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
